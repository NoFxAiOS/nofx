jobs:
  sync-official:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout our repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}  # ← 这行很重要
        fetch-depth: 0
